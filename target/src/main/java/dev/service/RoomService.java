package dev.service;

import dev.domain.Hotel;
import dev.domain.Room;
import dev.repository.HotelRepository;
import dev.repository.RoomRepository;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;
@Service
@Transactional
public class RoomService {
    private RoomRepository roomRepository;

    public RoomService(RoomRepository roomRepository) {
        this.roomRepository = roomRepository;
    }

    public void create(Room room) {
        // You may want to remove this line, as the ID is generated by the database
        // room.setRoom(room.getRoom());
        roomRepository.create(room);
    }

    public List<Room> getAll() {
        return roomRepository.getAll();
    }

    public Room get(int roomId) {
        return roomRepository.get(roomId);
    }

    public void edit(Room updatedRoom) {
        roomRepository.edit(updatedRoom);
    }

    public void delete(int roomId) {
        roomRepository.delete(roomId);
    }
  /*  private RoomRepository roomRepository;

    public RoomService(RoomRepository roomRepository) {
        this.roomRepository = roomRepository;
    }

    public void create(Room room) {
        room.setRoom(room.getRoom());
        roomRepository.create(room);
    }

    public List<Room> getAll() {
        return roomRepository.getAll();
    }

    public Room get(int room) {
        return roomRepository.get(room);
    }
    public void edit(Room room) {
        roomRepository.edit(room);
    }


    public void delete(int room) {
        roomRepository.delete(room);
    }*/


}
